<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title>edit</title>
</head>
<body>
  <h1>メモアプリ</h1>
  <h2>メモの変更</h2>
  <form action="/memos/<%= @id %>" method="POST">
    <div>
      <div class="form-group">
        <input type="text" class="form-control" name='title' value="<%= @title %>">
      </div>
      <div class="form-group">
        <textarea class="form-control h-300" name='content'><%= @content %></textarea>
      </div>
      <div>
        <input type="hidden" name="_method" value="PATCH">
        <input type="submit" class="btn btn-primary d-grid gap-2 col-6 mx-auto" value="変更">
      </div>
    </div>
  </form>
  <form action="/memos/<%= @id %>" method="GET">
    <div class="d-grid gap-2 col-6 mx-auto">
      <input type="submit" class="btn btn-secondary" value="戻る">
    </div>
  </form>
</body>
</html>
